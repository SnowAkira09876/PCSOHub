package com.ph.pcsolottowatcher.dialogs.years;

import com.ph.pcsolottowatcher.common.dialog.BaseDialogPresenter;

public class DialogFragmentYearsPresenter extends BaseDialogPresenter<DialogFragmentYearsView> {

  public DialogFragmentYearsPresenter(DialogFragmentYearsView view) {
    super(view);
  }
}
